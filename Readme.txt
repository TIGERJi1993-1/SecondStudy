This is for SJ study.
ADD line2.
add a new branch by calling "git checkout -b <name> equals to firstly git branck <name> and then git checkout <name>"
also git checkout -b <name> equals to git switch -c <name>

Add on feature1"Creating a new branch is quick AND simple."